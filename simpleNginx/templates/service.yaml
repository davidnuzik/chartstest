apiVersion: v1
kind: Service
metadata:
  name: {{ template "simpleNginx.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ template "simpleNginx.name" . }}
    helm.sh/chart: {{ template "simpleNginx.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ template "simpleNginx.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
